<!DOCTYPE html>
<html>
<head>
  <title>PixelSoul — Your Gamer Avatar</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    body { font-family: 'Segoe UI', sans-serif; background: #0f0c29; color: white; text-align: center; padding: 40px; }
    .upload-btn { background: #ff00cc; color: white; padding: 12px 24px; border: none; border-radius: 30px; font-size: 18px; cursor: pointer; }
    .style-grid { display: flex; flex-wrap: wrap; justify-content: center; gap: 20px; margin: 30px 0; }
    .style-card { width: 120px; background: #1a1a2e; padding: 10px; border-radius: 10px; cursor: pointer; }
    .preview { margin: 30px 0; }
    video { max-width: 300px; border-radius: 16px; box-shadow: 0 0 20px rgba(255,0,200,0.5); }
  </style>
</head>
<body>
  <h1>PixelSoul</h1>
  <p>Загрузи фото — стань героем своей игры</p>
  
  <input type="file" id="photoInput" accept="image/*" style="display:none">
  <button class="upload-btn" onclick="document.getElementById('photoInput').click()">📸 Загрузить фото</button>

  <div id="styleSelector" style="display:none;">
    <h3>Выбери стиль:</h3>
    <div class="style-grid">
      <div class="style-card" onclick="generate('cyberpunk')">🔴 Neon Hacker</div>
      <div class="style-card" onclick="generate('anime')">🎌 Anime Waifu</div>
      <div class="style-card" onclick="generate('fantasy')">⚔️ Dark Fantasy</div>
      <div class="style-card" style="opacity:0.4">🕹️ Retro 8-bit (премиум)</div>
    </div>
  </div>

  <div class="preview" id="preview" style="display:none;">
    <video controls autoplay muted loop>
      <source src="demo.mp4" type="video/mp4">
    </video>
    <br>
    <button class="upload-btn" style="background:#00c896">💎 Убрать водяной знак — 29 ₽</button>
  </div>

  <script>
    document.getElementById('photoInput').addEventListener('change', function() {
      if (this.files.length) {
        document.getElementById('styleSelector').style.display = 'block';
      }
    });

    function generate(style) {
      // Здесь будет вызов API (пока демо)
      document.getElementById('preview').style.display = 'block';
      alert('В реальном приложении: генерация аватарки в стиле ' + style + '...');
    }
  </script>
</body>
</html>
